/*
What is the HTML DOM ? 
When a web page is loaded the browser create a Documnet Object Model of the page. The HTML DOM is a standard object model and 
programing interface for HTML. It hold/define
    ==> The HTML elements as an Object
    ==> The properties of all HTML elements 
    ==> The methods to access all HTML elemets ==> document.getElementByID(), document.querySelector(), 
    Note: selector: #IdName, .ClassName, tagName
    ==> The events for all HTML element.
In other wards: The HTML DOM is a standard for how to get, change, add, or delete HTML elements. 
*/